site_name = "lefthook-config"
site_title = "@sebastienrousseau/lefthook-config — Shareable Lefthook git hook configuration rules replacing heavy legacy hook managers."
site_description = "Shareable Lefthook git hook configuration rules replacing heavy legacy hook managers."
base_url = "https://config.sebastienrousseau.com/lefthook-config"
language = "en-GB"

content_dir = "web/content"
template_dir = "web/_layouts"
output_dir = "public"

[agents]
agents_txt = true
ai_plugin = true

[agents.mcp]
enabled = true
